name: 'Setup Backend Linting Tools'
description: 'Install Python linting tools for backend (now a no-op as tools are in requirements-dev.txt)'

runs:
  using: 'composite'
  steps:
    - name: Verify linting tools are installed
      shell: bash
      run: |
        echo "✓ Linting tools should be installed via requirements-dev.txt"
        python --version
        black --version
        isort --version
        pylint --version
